server:
  port: 8206

spring:
  application:
    name: service-product
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.123.132:8848
      config:
        server-addr: 192.168.123.132:8848
        prefix: ${spring.application.name}
        file-extension: yaml
        shared-configs:
          - data-id: common.yaml
